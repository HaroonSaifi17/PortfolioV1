import { ScrollEffectDirective } from './scroll-effect.directive';

describe('ScrollEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollEffectDirective();
    expect(directive).toBeTruthy();
  });
});
